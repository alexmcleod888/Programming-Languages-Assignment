how to compile:
    - lex sorter.l
    - yacc -d sorter.y
    - cc lex.yy.c y.tab.c -o sorter

how to run:
    ./sorter

input:

list must be enter in the following format:
    [<number>, <number>, .....]

e.g.
    [2, 2, 1, 3, 2]
